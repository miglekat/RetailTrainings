<%= form_for(training) do |f| %>
  <% if training.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(training.errors.count, "error") %> prohibited this training from being saved:</h2>

      <ul>
      <% training.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Store_Name %>
    <%= f.text_field :Store_Name %>
  </div>

  <div class="field">
    <%= f.label :Store_Adress %>
    <%= f.text_area :Store_Adress %>
  </div>

  <div class="field">
    <%= f.label :Training_Room %>
    <%= f.text_area :Training_Room %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
